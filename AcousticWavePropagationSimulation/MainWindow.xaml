<Window x:Class="AcousticWavePropagationSimulation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AcousticWavePropagationSimulation"
        mc:Ignorable="d"
        Title="MainWindow" MinHeight="400" MinWidth="400" Height="1200" Width="1200">
    <Grid>
        <Image x:Name="Visualizer" Source="{Binding Image}"/>
        <Slider x:Name="PropagationSpeed" Value="{Binding PropagationSpeed}"
                Minimum="10"
                Maximum="5000"
                HorizontalAlignment="Center"
                Margin="50, 0, 50, 100"
                VerticalAlignment="Bottom"
                MinWidth="500"
                />
        
        <Button Margin="0, 50, 0, 0" VerticalAlignment="Top" Width="200" Height="50" Command="{Binding ToggleVisualizationCommand}">Toggle Visualization</Button>


    </Grid>
</Window>
